# asm-ext

## Util

- Classfile metadata(used methods/classes/fields/annotations)
- Compile utils for custom compilers
- Classloader thats accepts bytecode
- Annotation nodes stuff
- Post-Visitors (runs callbacks after full node visitation)  
- Multi-Visitors (executes multiple visitors in single pass)  

## Transformation 

Implements some transformation pipeline

## Analitics

- Data Flow Analyzer
- Method Return Type Inference
- Bytecode Region Mapping (for object initialization expressions) 
